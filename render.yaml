services:
  - type: background
    name: moon-or-dust-bot
    env: python
    pythonVersion: 3.11.10  # Compatible version for aiogram + aiohttp
    buildCommand: "pip install --no-cache-dir -r requirements.txt"
    startCommand: "python bot.py"
    autoDeploy: true
    branch: main
